[workspace]

members = [
    "parslers-macro",
    "parslers-lib",
    "parslers-json",
    "parslers-benchmark",
    "parslers-branflakes"
]

[profile.release]
strip = true
lto = true
panic = "abort"