[workspace]

members = [
    "parslers-macro",
    "parslers-lib",
    "parslers-json",
    "parslers-benchmark",
    "parslers-branflakes",
    "parslers-wacc",
    "parslers-example"
]

[profile.release]
strip = true
lto = true
panic = "abort"