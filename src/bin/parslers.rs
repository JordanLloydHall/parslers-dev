use parslers::*;

// parsler!(
//     let parker = (('a''b') | ('a''c'))
// );

fn main() {
    // println!("{:?}", parsler("ab"));
}

// #[cfg(test)]
// mod tests {
//     use super::*;

//     #[test]
//     fn it_works() {
//         assert_eq!(parsler("ab"), Ok((('a', 'b'), "")));
//     }
// }
